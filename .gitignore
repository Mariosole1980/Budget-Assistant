node_modules

# Build artifacts
www/
android/app/build/
android/.gradle/
android/build/
*.apk
*.zip
*.aab

# Cloudflare / deployment caches
.wrangler/
.netlify/
.uploads/

# Local environment / secrets
.capgo_key_v2
.capgo_key_v2.pub
release.keystore

# Local development env vars (wrangler/netlify) — NEVER commit real keys
.dev.vars
.env
.env.*

# Archive of stale duplicates (kept for reference, not part of the app)
archive/

# Scratch / diagnostic scripts (development only)
scratch/

# Recurring recovery diagnostic artifacts (development only)
recurring-recovery-*.sql
recurring-recovery-*.js
recurring-recovery-*.md

# OTA boot/engine modules (generated build artifacts produced by the native/OTA
# release step; must never be swept into a release commit)
ota-boot-loader.js
js/OTAEngine.js
